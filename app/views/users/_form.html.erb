<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id, id: :user_id %>
  </div>

  <div class="field">
    <%= form.label :imei %>
    <%= form.text_field :imei, id: :user_imei %>
  </div>

  <div class="field">
    <%= form.label :contacts %>
    <%= form.text_field :contacts, id: :user_contacts %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
